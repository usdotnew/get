{
  "private": true,
  "name": "get",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "RunLaszloRun <34609082+RunLaszloRun@users.noreply.github.com>",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
